/**
 * {{toValidIdentifier entityName}}DTO - generated from ontology.json
 * DO NOT EDIT MANUALLY. Changes will be overwritten.
 */

export interface {{toValidIdentifier entityName}}DTO {
  id: string;
  type: string;
  label: string;
  {{#each properties}}
  {{@key}}?: {{{replaceType this}}};
  {{/each}}
  enrichedData?: any;
  createdAt?: Date | string;
  updatedAt?: Date | string;
}

export function is{{toValidIdentifier entityName}}DTO(obj: any): obj is {{toValidIdentifier entityName}}DTO {
  return obj && 
         typeof obj.id === 'string' && 
         typeof obj.type === 'string' && 
         typeof obj.label === 'string';
}

export function create{{toValidIdentifier entityName}}DTO(data: Partial<{{toValidIdentifier entityName}}DTO>): {{toValidIdentifier entityName}}DTO {
  return {
    id: data.id || '',
    type: data.type || '{{toValidIdentifier entityName}}',
    label: data.label || '{{toValidIdentifier entityName}}',
    {{#each properties}}
    {{@key}}: data.{{@key}},
    {{/each}}
    enrichedData: data.enrichedData,
    createdAt: data.createdAt || new Date(),
    updatedAt: data.updatedAt || new Date()
  };
} 