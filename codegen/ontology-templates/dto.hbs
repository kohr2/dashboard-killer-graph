/**
 * {{entityName}}DTO - generated from ontology.json
 * DO NOT EDIT MANUALLY. Changes will be overwritten.
 */

export interface {{entityName}}DTO {
  id: string;
  type: string;
  label: string;
  {{#each properties}}
  {{@key}}?: {{{replaceType this}}};
  {{/each}}
  enrichedData?: any;
  createdAt?: Date | string;
  updatedAt?: Date | string;
}

export function is{{entityName}}DTO(obj: any): obj is {{entityName}}DTO {
  return obj && 
         typeof obj.id === 'string' && 
         typeof obj.type === 'string' && 
         typeof obj.label === 'string';
}

export function create{{entityName}}DTO(data: Partial<{{entityName}}DTO>): {{entityName}}DTO {
  return {
    id: data.id || '',
    type: data.type || '{{entityName}}',
    label: data.label || '{{entityName}}',
    {{#each properties}}
    {{@key}}: data.{{@key}},
    {{/each}}
    enrichedData: data.enrichedData,
    createdAt: data.createdAt || new Date(),
    updatedAt: data.updatedAt || new Date()
  };
} 