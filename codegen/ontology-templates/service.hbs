import { singleton } from 'tsyringe';
import { {{toValidIdentifier entityName}} } from './{{toValidIdentifier entityName}}.entity';
import { I{{toValidIdentifier entityName}}Repository } from './{{toValidIdentifier entityName}}.repository';
import { logger } from '@shared/utils/logger';

@singleton()
export class {{toValidIdentifier entityName}}Service {
  constructor(private repository: I{{toValidIdentifier entityName}}Repository) {}

  /**
   * Find entity by ID
   */
  async findById(id: string): Promise<{{toValidIdentifier entityName}} | null> {
    try {
      return await this.repository.findById(id);
    } catch (error) {
      logger.error(`Error finding {{toValidIdentifier entityName}} by ID ${id}:`, error);
      return null;
    }
  }

  /**
   * Get all entities
   */
  async findAll(): Promise<{{toValidIdentifier entityName}}[]> {
    try {
      return await this.repository.findAll();
    } catch (error) {
      logger.error('Error finding all {{toValidIdentifier entityName}} entities:', error);
      return [];
    }
  }

  /**
   * Create new entity
   */
  async create(entity: {{toValidIdentifier entityName}}): Promise<{{toValidIdentifier entityName}} | null> {
    try {
      return await this.repository.create(entity);
    } catch (error) {
      logger.error('Error creating {{toValidIdentifier entityName}}:', error);
      return null;
    }
  }

  /**
   * Update existing entity
   */
  async update(id: string, entity: Partial<{{toValidIdentifier entityName}}>): Promise<{{toValidIdentifier entityName}} | null> {
    try {
      return await this.repository.update(id, entity);
    } catch (error) {
      logger.error(`Error updating {{toValidIdentifier entityName}} ${id}:`, error);
      return null;
    }
  }

  /**
   * Delete entity
   */
  async delete(id: string): Promise<boolean> {
    try {
      await this.repository.delete(id);
      return true;
    } catch (error) {
      logger.error(`Error deleting {{toValidIdentifier entityName}} ${id}:`, error);
      return false;
    }
  }

  {{#if keyProperties}}
  /**
   * Find entity by key properties
   */
  async findBy{{#each keyProperties}}{{#if @first}}{{this}}{{else}}And{{this}}{{/if}}{{/each}}({{#each keyProperties}}{{this}}: string{{#unless @last}}, {{/unless}}{{/each}}): Promise<{{toValidIdentifier entityName}} | null> {
    try {
      return await this.repository.findBy{{#each keyProperties}}{{#if @first}}{{this}}{{else}}And{{this}}{{/if}}{{/each}}({{#each keyProperties}}{{this}}{{#unless @last}}, {{/unless}}{{/each}});
    } catch (error) {
      logger.error(`Error finding {{toValidIdentifier entityName}} by key properties:`, error);
      return null;
    }
  }
  {{/if}}

  {{#if vectorIndex}}
  /**
   * Find similar entities
   */
  async findSimilar(entity: {{toValidIdentifier entityName}}, limit?: number): Promise<{{toValidIdentifier entityName}}[]> {
    try {
      return await this.repository.findSimilar(entity, limit);
    } catch (error) {
      logger.error('Error finding similar {{toValidIdentifier entityName}} entities:', error);
      return [];
    }
  }
  {{/if}}
} 