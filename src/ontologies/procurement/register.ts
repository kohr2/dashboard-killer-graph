import { container } from 'tsyringe';

/**
 * Registers the providers for the Procurement ontology.
 * Currently a placeholder.
 */
export function registerProcurement(): void {
  // No services to register yet for this ontology.
} 